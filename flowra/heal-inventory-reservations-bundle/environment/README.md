# Inventory Reservation Service

Internal microservice used by cart and checkout teams. Holds stock while items sit in a shopping cart.

See `inventory/service.py` for the API. Storage is SQLite via `inventory/storage.py`.
